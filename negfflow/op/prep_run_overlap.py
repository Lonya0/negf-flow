from dflow import Steps

class PrepRunOverlap(Steps):
    def __init__(self, name, prep_op, run_op, prep_config, run_config, upload_python_packages):
        super().__init__(name)
        return
